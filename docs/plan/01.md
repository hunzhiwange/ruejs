# 01：定义 Renderable 核心类型

## 目标

先把新协议的名字和边界定下来，只做类型骨架，不改变运行时行为。

## 只做这些事

1. 新增 RenderTarget、Renderable、BlockFactory、BlockInstance 等核心类型。
2. 让这些类型可以从 runtime 主入口导出。
3. 补最小类型级测试或类型检查用例。

## 建议涉及文件

1. packages/runtime/src/renderable.ts
2. packages/runtime/src/index.ts
3. 必要时新增最小测试文件

## 不要顺手做

1. 不改 render / renderBetween。
2. 不改 swc-plugin-rue。
3. 不改 runtime-vapor Rust 逻辑。

## 完成标准

1. 类型命名稳定、职责清晰。
2. 旧 API 行为完全不变。
3. pnpm check 通过。

## 回退点

如果命名或抽象不稳定，只保留内部导出，不要立刻大面积引用。

\*\*\* Add File: /Users/Shared/work/dir/data/codes/rue/docs/plan/02.md

# 02：实现 normalizeRenderable

## 目标

建立统一的输入规范化入口，把不同形态的 children / 返回值收敛到新协议上。

## 只做这些事

1. 新增 normalizeRenderable。
2. 先支持字符串、数字、DOM 节点、DocumentFragment、数组、Block。
3. 为后续 compat VNode 预留分支，但本步不要求完成 compat 适配。

## 建议涉及文件

1. packages/runtime/src/renderable-normalize.ts
2. packages/runtime/src/renderable.ts
3. 必要的最小测试文件

## 不要顺手做

1. 不修改 render / renderBetween 入口。
2. 不迁移 vaporCreateVNode 调用点。
3. 不引入 Router 或 built-ins 改动。

## 完成标准

1. normalizeRenderable 可独立测试。
2. 数组和文本输入的规范化规则固定下来。
3. pnpm check 与对应测试通过。

## 回退点

如果数组或 DOM 语义拿不准，先支持最小集合，不要提前兼容所有历史输入。

\*\*\* Add File: /Users/Shared/work/dir/data/codes/rue/docs/plan/03.md

# 03：桥接 render 与 renderBetween

## 目标

让现有渲染入口可以识别 Renderable，但仍然兼容旧 VNode 路径。

## 只做这些事

1. 在 render / renderBetween 中接入 normalizeRenderable。
2. 新协议走新入口，旧 VNode 继续走旧桥接。
3. 保持业务层 API 不变。

## 建议涉及文件

1. packages/runtime/src/rue.ts
2. packages/runtime/src/index.ts
3. packages/runtime-vapor/src/runtime/bridge/render.rs

## 不要顺手做

1. 不做 Block 清理机制重构。
2. 不改 createElement / h。
3. 不改编译器输出。

## 完成标准

1. render 和 renderBetween 可消费 Renderable。
2. 旧 VNode 调用不回归。
3. 最小挂载与区间渲染测试通过。

## 回退点

如果 Rust 入口改动过大，可以先在 TS 层桥接，再把 Rust 适配留到下一步。

\*\*\* Add File: /Users/Shared/work/dir/data/codes/rue/docs/plan/04.md

# 04：引入 Block 清理拥有者

## 目标

把副作用、事件和 effect scope 的清理责任，从 VNode before_unmount 抽到 BlockInstance 自身。

## 只做这些事

1. 为 BlockInstance 增加 cleanup bucket 或等价机制。
2. 支持在 unmount 时统一释放资源。
3. 为后续 Router 和 built-ins 迁移打基础。

## 建议涉及文件

1. packages/runtime/src/renderable-lifecycle.ts
2. packages/runtime-vapor/src/runtime/render_lifecycle.rs
3. 必要的响应式清理测试

## 不要顺手做

1. 不迁移 built-ins。
2. 不抽 compat adapter。
3. 不修改编译器。

## 完成标准

1. Block 卸载有独立清理路径。
2. 重复 mount / unmount 不会累积 effect。
3. 对现有 VNode 生命周期不造成行为破坏。

## 回退点

如果一次性完全替换风险过大，允许先做并行清理机制，再逐步切换主路径。

\*\*\* Add File: /Users/Shared/work/dir/data/codes/rue/docs/plan/05.md

# 05：建立 compat VNode 适配器

## 目标

把旧 VNode 正式收缩为 compat 路径，而不是继续和新协议混在一起。

## 只做这些事

1. 新增 VNode 到 Renderable 的 compat adapter。
2. 让旧 h / createElement 产物通过该 adapter 进入新入口。
3. 不改变业务层旧调用方式。

## 建议涉及文件

1. packages/runtime/src/renderable-compat.ts
2. packages/runtime/src/rue.ts
3. packages/runtime-vapor/src/runtime/bridge/create_element.rs

## 不要顺手做

1. 不改 swc-plugin-rue。
2. 不切公开类型。
3. 不删除旧的 createElement / patch 逻辑。

## 完成标准

1. compat 边界清晰可见。
2. 新协议与旧 VNode 混用时仍然稳定。
3. 第一阶段结束后，后续模块迁移不再直接依赖裸 VNode 入口。

## 回退点

如果适配器实现复杂，先保证“可桥接”，不要在这一步强行清理所有旧逻辑。

\*\*\* Add File: /Users/Shared/work/dir/data/codes/rue/docs/plan/06.md

# 06：编译器 imports 与 slot / children 规范化

## 目标

先把编译器最容易收敛的输入口改掉：imports、slot 和 children 规范化。

## 只做这些事

1. 调整 swc-plugin-rue 的运行时导入，改为依赖新协议入口。
2. 替换 slot / children 中对 \_$vaporCreateVNode 和 h('fragment', ...) 的依赖。
3. 只覆盖 slot / children，不碰组件边界和列表主逻辑。

## 建议涉及文件

1. packages/swc-plugin-rue/src/imports.rs
2. packages/swc-plugin-rue/src/element_slot.rs
3. 必要时补 children 相关帮助模块

## 不要顺手做

1. 不改 Router。
2. 不改 Teleport / Transition。
3. 不一次性处理 component、list、expr container。

## 完成标准

1. 编译输出中 slot 路径不再默认依赖 \_$vaporCreateVNode。
2. slot / children 编译快照稳定。
3. swc-plugin-rue 的 test 和 check 通过。

## 回退点

如果 children 规范化范围太大，先只处理 slot，children 继续走兼容路径。

\*\*\* End Patch
