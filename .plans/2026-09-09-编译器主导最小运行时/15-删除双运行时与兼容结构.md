# 任务 15: 删除双运行时与兼容结构

批次：【批次 15】 依赖批次：依赖批次 14

状态：未开始

目的：物理删除 js-runtime、legacy/portable/render fallback 和聚合公开入口，确保没有隐藏恢复路径。

来源任务：任务 14

预计会话范围：删除已零调用文件、收紧 exports/build/release，并修正只因删除产生的配置与测试引用。

## 文件

- 删除：`packages/runtime/src/runtime-core/js-runtime/**`
- 删除：`packages/runtime/src/compiled-legacy-dom.ts`
- 删除：`packages/runtime/src/compiled-dom-bindings-legacy.ts`
- 删除：`packages/runtime/src/compiled-render-anchor.ts`
- 删除：`packages/runtime/src/compiled-dynamic.ts`
- 删除：`packages/runtime/src/public/rendering.ts`
- 修改：`packages/runtime/src/rue.ts`
- 修改：`packages/runtime/package.json`
- 修改：`packages/rue/package.json`
- 修改：`scripts/build.js`
- 修改：`scripts/check-compiler-runtime-boundary.js`

## 上下文

- 删除前必须由 RustCodeGraph callers 和仓库扫描共同证明生产调用为零。
- 允许删除 `render`、运行时 JSX、legacy handle、portable renderable、runtime registry 等公开能力；不提供 deprecated shim。

## 测试计划

- 行为：包 exports 只暴露编译器驱动入口；旧入口解析失败，新入口构建与执行成功。
- 失败验证测试：package contract 测试先断言旧 exports 不应存在、禁止目录必须不存在。
- 失败验证命令：`pnpm exec vitest run --project unit scripts/__tests__/workspace-esm-package-contract.spec.ts scripts/__tests__/compiler-only-runtime-audit.spec.ts`
- 预期失败原因：旧文件、exports 和 build entry 仍存在。
- 通过验证命令：同上，加 `pnpm run check` 与 package build。
- 模拟策略：真实 package exports 与打包产物。

## 步骤

1. 获取每个删除目标的零生产调用证据。
2. 先写 package/文件系统失败契约。
3. 删除目录和文件，并移除 exports/build entry/re-export。
4. 将最小 bootstrap API 留在 compiler-runtime app entry，不复用 `rue.ts` 通用 renderer。
5. 强化边界脚本，未来重新引入禁用目录或 token 时失败。

## 验证

- 运行：失败验证命令。
- 运行：`pnpm run prepare-unit-test-artifacts && pnpm run check`
- 运行：`node scripts/compiler-only-runtime-audit.js --check`
- 预期：删除目标不存在、旧 exports 解析失败、所有体积目标通过。
- 所需证据：零调用、删除清单、package tarball 文件、体积模块清单和退出码。

## 完成

若仍有真实调用，应回到来源任务迁移，不能在本任务增加桥接文件。
