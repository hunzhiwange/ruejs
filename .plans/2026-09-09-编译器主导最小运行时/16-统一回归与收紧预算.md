# 任务 16: 统一回归与收紧预算

批次：【批次 16】 依赖批次：依赖批次 15

状态：未开始

目的：用完整功能、产物依赖和体积证据证明单一 compiled 架构完成，并将实际值固化为更严预算。

来源任务：任务 15

预计会话范围：只做最终构建、审计、预算、调查报告和回归，不新增架构实现。

## 文件

- 修改：`scripts/runtime-size-baseline.json`
- 修改：`scripts/runtime-size-budget.json`
- 修改：`scripts/runtime-size-investigation.md`
- 修改：`scripts/__tests__/compiler-only-runtime-audit.spec.ts`
- 修改：`scripts/release.js`

## 上下文

- 最终稳定值小于目标时预算锁到稳定值；完成必须证明禁用模块不存在、入口无泄漏、未编译 JSX 明确失败。

## 测试计划

- 行为：同一源码快照两次构建/审计一致，全部 compiler-only 场景、测试、SSR/hydration 和 release 门禁通过。
- 失败验证：将任一 fixture 预算设为最终值减 1，证明检查退出码非 0，再恢复实际值。
- 失败验证命令：`node scripts/compiler-only-runtime-audit.js --check`
- 预期失败原因：人工低 1 B fixture 超预算。
- 通过验证命令：恢复实际预算后运行全部下列命令。
- 模拟策略：真实生产构建、压缩器和完整测试。

## 步骤

1. 确认无并发写入并完整重建 SWC、runtime、rue、server-renderer 与生态包。
2. 连续运行两次审计并逐字节比较。
3. 审查 resolved entry、rendered modules、四项尺寸与禁止依赖。
4. 收紧预算并保留原始 baseline 供累计比较。
5. 运行统一回归，更新调查报告和破坏性 API 说明。

## 验证

- 运行：`pnpm --filter @rue-js/swc-plugin-rue test && pnpm --filter @rue-js/swc-plugin-rue run build`
- 运行：`pnpm run prepare-unit-test-artifacts && pnpm run check`
- 运行：`node scripts/compiler-only-runtime-audit.js --check && pnpm run size:tree-shaking:check`
- 运行：`pnpm exec vitest run --project unit --project unit-jsdom`
- 运行：`pnpm run test-e2e`
- 预期：全部退出码 0；static ≤1 KB、reactive-text ≤3 KB、component ≤10 KB、list ≤5 KB、Teleport/Transition ≤12 KB、hydrate ≤25 KB；禁止模块为零。
- 所需证据：命令、退出码、测试数量、两次报告 cmp、逐场景尺寸、模块删除和 diff 审查。

## 完成

仅全部证据齐备才能删除任务文件；无关失败按技能契约保留为“已完成，待回归”。
