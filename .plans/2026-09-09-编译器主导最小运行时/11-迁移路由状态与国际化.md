# 任务 11: 迁移路由、状态与国际化

批次：【批次 11】 依赖批次：依赖批次 10

状态：未开始

目的：将 router、store、i18n 的旧渲染入口和任意 children 协议迁到 compiler-only ABI。

来源任务：任务 10

预计会话范围：三个基础包共享应用上下文与编译入口，范围限于包内生产源码和现有测试。

## 文件

- 修改：`packages/router/src/**`
- 修改：`packages/store/src/**`
- 修改：`packages/i18n/src/**`
- 测试：`packages/router/__tests__/router.spec.tsx`
- 测试：`packages/store/__tests__/storeApp.spec.tsx`
- 测试：`packages/i18n/__tests__/i18n.spec.tsx`

## 上下文

- 动态 UI 必须使用 compiled branch/slot/data；核心 ABI 已冻结，禁止新增包内 renderer 或 adapter。

## 测试计划

- 行为：导航生命周期、应用状态和国际化组件在 closed component/slot ABI 下工作。
- 失败验证测试：三包生产源码不得导入旧 render、runtime JSX、portable/legacy helper。
- 失败验证命令：`pnpm exec vitest run --project unit packages/router/__tests__ packages/store/__tests__ packages/i18n/__tests__`
- 预期失败原因：现有生产调用仍使用旧入口或任意 children。
- 通过验证命令：同上，并构建三个包。
- 模拟策略：沿用现有浏览器边界 mock，编译与渲染使用真实实现。

## 步骤

1. 添加旧符号零命中扫描。
2. 迁移 router view、store app 绑定和 i18n 组件。
3. 把动态 children 改为静态 slot 或显式数据。
4. 分别构建测试，禁止复制 runtime。
5. 用 RustCodeGraph 确认旧符号生产调用为零。

## 验证

- 运行：失败验证命令。
- 运行：`pnpm run check`
- 预期：测试与类型检查通过，三包旧调用为零。
- 所需证据：迁移清单、测试数量、退出码和调用图。

## 完成

发现 ABI 缺口应回到来源任务修正唯一实现，不能添加兼容层。
