# 任务 14: 迁移站点、文档与示例

批次：【批次 14】 依赖批次：依赖批次 13

状态：未开始

目的：迁移 app、docs 和 examples，验证真实应用只依赖编译器主导入口。

来源任务：任务 13

预计会话范围：只修改最终上层消费者、示例和说明，不修改核心 runtime ABI。

## 文件

- 修改：`app/**`
- 修改：`docs/**`
- 修改：`examples/**`
- 测试：现有应用与 server-islands E2E

## 上下文

- 所有入口必须经过 Vite/SWC；动态组件重写为可枚举 branch，文档不得继续宣传 runtime JSX/render。

## 测试计划

- 行为：文档站、示例和 server islands 能生产构建并完成关键交互。
- 失败验证测试：生产源码旧符号扫描和现有 E2E。
- 失败验证命令：`pnpm run build && pnpm run test-e2e`
- 预期失败原因：上层入口与示例仍展示或调用旧 API。
- 通过验证命令：同上。
- 模拟策略：真实生产构建和浏览器 E2E。

## 步骤

1. 迁移应用 bootstrap 与页面动态 UI。
2. 更新文档为 compiler-required、closed ABI 和不支持路径。
3. 迁移所有示例，删除 legacy/portable 示例。
4. 运行构建/E2E并修正调用方问题。
5. 确认整个工作区生产源码旧调用为零。

## 验证

- 运行：`pnpm run build`
- 运行：`pnpm run test-e2e`
- 预期：全部通过且仓库扫描为零。
- 所需证据：构建/E2E 退出码、页面场景和零命中结果。

## 完成

不得通过关闭编译优化或启用 fallback 迁移示例。
