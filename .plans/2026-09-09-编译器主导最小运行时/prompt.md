# 编译器主导最小运行时任务提示词

按顺序复制以下提示到独立对话框执行。执行前先阅读 `plan.md` 和对应任务文件；`plan.md` 只是整体架构指南，不是进度来源，不要修改它的任何部分。

# 批次 1

## 任务 1: 固化破坏性目标与门禁【批次 1】

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-09-09-编译器主导最小运行时/1-固化破坏性目标与门禁.md`。

开始前先阅读 `.plans/2026-09-09-编译器主导最小运行时/plan.md` 和该任务文件，确认依赖已满足。按任务文件中的测试驱动开发/验证计划实施，尽可能只处理该任务范围，遇到阻塞可以扩大任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的问题导致无法运行验证，可以修改为 `已完成，待回归` 并记录待回归命令。
注意，`.plans/2026-09-09-编译器主导最小运行时/plan.md` 为只读，不要修改该文件的任何部分。
```

# 批次 2

## 任务 2: 统一编译产物协议与精确导入【批次 2】依赖批次 1

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-09-09-编译器主导最小运行时/2-统一编译产物协议与精确导入.md`。

开始前先阅读 `.plans/2026-09-09-编译器主导最小运行时/plan.md` 和该任务文件，确认依赖已满足。按任务文件中的测试驱动开发/验证计划实施，尽可能只处理该任务范围，遇到阻塞可以扩大任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的问题导致无法运行验证，可以修改为 `已完成，待回归` 并记录待回归命令。
注意，`.plans/2026-09-09-编译器主导最小运行时/plan.md` 为只读，不要修改该文件的任何部分。
```

# 批次 3

## 任务 3: 编译静态 DOM 与定点更新【批次 3】依赖批次 2

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-09-09-编译器主导最小运行时/3-编译静态DOM与定点更新.md`。

开始前先阅读 `.plans/2026-09-09-编译器主导最小运行时/plan.md` 和该任务文件，确认依赖已满足。按任务文件中的测试驱动开发/验证计划实施，尽可能只处理该任务范围，遇到阻塞可以扩大任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的问题导致无法运行验证，可以修改为 `已完成，待回归` 并记录待回归命令。
注意，`.plans/2026-09-09-编译器主导最小运行时/plan.md` 为只读，不要修改该文件的任何部分。
```

# 批次 4

## 任务 4: 闭合 Block 与控制流【批次 4】依赖批次 3

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-09-09-编译器主导最小运行时/4-闭合Block与控制流.md`。

开始前先阅读 `.plans/2026-09-09-编译器主导最小运行时/plan.md` 和该任务文件，确认依赖已满足。按任务文件中的测试驱动开发/验证计划实施，尽可能只处理该任务范围，遇到阻塞可以扩大任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的问题导致无法运行验证，可以修改为 `已完成，待回归` 并记录待回归命令。
注意，`.plans/2026-09-09-编译器主导最小运行时/plan.md` 为只读，不要修改该文件的任何部分。
```

# 批次 5

## 任务 5: 编译列表与插槽【批次 5】依赖批次 4

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-09-09-编译器主导最小运行时/5-编译列表与插槽.md`。

开始前先阅读 `.plans/2026-09-09-编译器主导最小运行时/plan.md` 和该任务文件，确认依赖已满足。按任务文件中的测试驱动开发/验证计划实施，尽可能只处理该任务范围，遇到阻塞可以扩大任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的问题导致无法运行验证，可以修改为 `已完成，待回归` 并记录待回归命令。
注意，`.plans/2026-09-09-编译器主导最小运行时/plan.md` 为只读，不要修改该文件的任何部分。
```

# 批次 6

## 任务 6: 闭合组件与 Props 协议【批次 6】依赖批次 5

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-09-09-编译器主导最小运行时/6-闭合组件与Props协议.md`。

开始前先阅读 `.plans/2026-09-09-编译器主导最小运行时/plan.md` 和该任务文件，确认依赖已满足。按任务文件中的测试驱动开发/验证计划实施，尽可能只处理该任务范围，遇到阻塞可以扩大任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的问题导致无法运行验证，可以修改为 `已完成，待回归` 并记录待回归命令。
注意，`.plans/2026-09-09-编译器主导最小运行时/plan.md` 为只读，不要修改该文件的任何部分。
```

# 批次 7

## 任务 7: 最小响应式与 Hooks 编译【批次 7】依赖批次 6

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-09-09-编译器主导最小运行时/7-最小响应式与Hooks编译.md`。

开始前先阅读 `.plans/2026-09-09-编译器主导最小运行时/plan.md` 和该任务文件，确认依赖已满足。按任务文件中的测试驱动开发/验证计划实施，尽可能只处理该任务范围，遇到阻塞可以扩大任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的问题导致无法运行验证，可以修改为 `已完成，待回归` 并记录待回归命令。
注意，`.plans/2026-09-09-编译器主导最小运行时/plan.md` 为只读，不要修改该文件的任何部分。
```

# 批次 8

## 任务 8: 内置组件编译为专用原语【批次 8】依赖批次 7

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-09-09-编译器主导最小运行时/8-内置组件编译为专用原语.md`。

开始前先阅读 `.plans/2026-09-09-编译器主导最小运行时/plan.md` 和该任务文件，确认依赖已满足。按任务文件中的测试驱动开发/验证计划实施，尽可能只处理该任务范围，遇到阻塞可以扩大任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的问题导致无法运行验证，可以修改为 `已完成，待回归` 并记录待回归命令。
注意，`.plans/2026-09-09-编译器主导最小运行时/plan.md` 为只读，不要修改该文件的任何部分。
```

# 批次 9

## 任务 9: 编译 SSR 与 Hydration 指令【批次 9】依赖批次 8

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-09-09-编译器主导最小运行时/9-编译SSR与Hydration指令.md`。

开始前先阅读 `.plans/2026-09-09-编译器主导最小运行时/plan.md` 和该任务文件，确认依赖已满足。按任务文件中的测试驱动开发/验证计划实施，尽可能只处理该任务范围，遇到阻塞可以扩大任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的问题导致无法运行验证，可以修改为 `已完成，待回归` 并记录待回归命令。
注意，`.plans/2026-09-09-编译器主导最小运行时/plan.md` 为只读，不要修改该文件的任何部分。
```

# 批次 10

## 任务 10: 强制编译入口与应用启动【批次 10】依赖批次 9

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-09-09-编译器主导最小运行时/10-强制编译入口与应用启动.md`。

开始前先阅读 `.plans/2026-09-09-编译器主导最小运行时/plan.md` 和该任务文件，确认依赖已满足。按任务文件中的测试驱动开发/验证计划实施，尽可能只处理该任务范围，遇到阻塞可以扩大任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的问题导致无法运行验证，可以修改为 `已完成，待回归` 并记录待回归命令。
注意，`.plans/2026-09-09-编译器主导最小运行时/plan.md` 为只读，不要修改该文件的任何部分。
```

# 批次 11

## 任务 11: 迁移路由、状态与国际化【批次 11】依赖批次 10

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-09-09-编译器主导最小运行时/11-迁移路由状态与国际化.md`。

开始前先阅读 `.plans/2026-09-09-编译器主导最小运行时/plan.md` 和该任务文件，确认依赖已满足。按任务文件中的测试驱动开发/验证计划实施，尽可能只处理该任务范围，遇到阻塞可以扩大任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的问题导致无法运行验证，可以修改为 `已完成，待回归` 并记录待回归命令。
注意，`.plans/2026-09-09-编译器主导最小运行时/plan.md` 为只读，不要修改该文件的任何部分。
```

# 批次 12

## 任务 12: 迁移 Text 全栈运行链【批次 12】依赖批次 11

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-09-09-编译器主导最小运行时/12-迁移Text全栈运行链.md`。

开始前先阅读 `.plans/2026-09-09-编译器主导最小运行时/plan.md` 和该任务文件，确认依赖已满足。按任务文件中的测试驱动开发/验证计划实施，尽可能只处理该任务范围，遇到阻塞可以扩大任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的问题导致无法运行验证，可以修改为 `已完成，待回归` 并记录待回归命令。
注意，`.plans/2026-09-09-编译器主导最小运行时/plan.md` 为只读，不要修改该文件的任何部分。
```

# 批次 13

## 任务 13: 迁移 Rue Design【批次 13】依赖批次 12

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-09-09-编译器主导最小运行时/13-迁移RueDesign.md`。

开始前先阅读 `.plans/2026-09-09-编译器主导最小运行时/plan.md` 和该任务文件，确认依赖已满足。按任务文件中的测试驱动开发/验证计划实施，尽可能只处理该任务范围，遇到阻塞可以扩大任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的问题导致无法运行验证，可以修改为 `已完成，待回归` 并记录待回归命令。
注意，`.plans/2026-09-09-编译器主导最小运行时/plan.md` 为只读，不要修改该文件的任何部分。
```

# 批次 14

## 任务 14: 迁移站点、文档与示例【批次 14】依赖批次 13

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-09-09-编译器主导最小运行时/14-迁移站点文档与示例.md`。

开始前先阅读 `.plans/2026-09-09-编译器主导最小运行时/plan.md` 和该任务文件，确认依赖已满足。按任务文件中的测试驱动开发/验证计划实施，尽可能只处理该任务范围，遇到阻塞可以扩大任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的问题导致无法运行验证，可以修改为 `已完成，待回归` 并记录待回归命令。
注意，`.plans/2026-09-09-编译器主导最小运行时/plan.md` 为只读，不要修改该文件的任何部分。
```

# 批次 15

## 任务 15: 删除双运行时与兼容结构【批次 15】依赖批次 14

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-09-09-编译器主导最小运行时/15-删除双运行时与兼容结构.md`。

开始前先阅读 `.plans/2026-09-09-编译器主导最小运行时/plan.md` 和该任务文件，确认依赖已满足。按任务文件中的测试驱动开发/验证计划实施，尽可能只处理该任务范围，遇到阻塞可以扩大任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的问题导致无法运行验证，可以修改为 `已完成，待回归` 并记录待回归命令。
注意，`.plans/2026-09-09-编译器主导最小运行时/plan.md` 为只读，不要修改该文件的任何部分。
```

# 批次 16

## 任务 16: 统一回归与收紧预算【批次 16】依赖批次 15

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-09-09-编译器主导最小运行时/16-统一回归与收紧预算.md`。

开始前先阅读 `.plans/2026-09-09-编译器主导最小运行时/plan.md` 和该任务文件，确认依赖已满足。按任务文件中的测试驱动开发/验证计划实施，尽可能只处理该任务范围，遇到阻塞可以扩大任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的问题导致无法运行验证，可以修改为 `已完成，待回归` 并记录待回归命令。
注意，`.plans/2026-09-09-编译器主导最小运行时/plan.md` 为只读，不要修改该文件的任何部分。
```
