# 任务 2: 压缩依赖图热路径

批次：【批次 2】 依赖批次 1

状态：未开始

目的：减少 Graph 节点、边、句柄和传播队列的对象分配，同时保持精确依赖、去重与释放安全。

来源任务：无

预计会话范围：聚焦 `ReactiveGraph` 数据结构与其直接测试；不修改 Signal API、编译器或公共导出。

## 文件

- 修改：`packages/runtime/src/runtime-core/reactive-kernel/graph.ts`
- 修改：`packages/runtime/__tests__/reactive-kernel.graph.spec.ts`
- 修改：`packages/runtime/__tests__/reactive-kernel.test-utils.ts`
- 参考：`packages/runtime/src/runtime-core/reactive-kernel/effect.ts`

## 上下文

- 当前 arena 为每个 ID、slot、node、link 分配对象。改为数字化代际句柄与并行数组/紧凑记录，保留 stale handle 防护、ordered link reuse、循环追踪和 diamond 去重。
- 不增加兼容包装对象；直接更新内部类型和调用点。

## 测试计划

- 行为：高频创建/移除后旧句柄永不指向复用节点，传播顺序和边数保持正确，分配指标下降。
- 失败验证测试：在 `reactive-kernel.graph.spec.ts` 新增“大规模代际复用与零重复传播”以及 debug 统计断言。
- 失败验证命令：`pnpm exec vitest run packages/runtime/__tests__/reactive-kernel.graph.spec.ts --project unit-jsdom`
- 预期失败原因：当前 Graph 不暴露所需紧凑存储统计，且对象句柄分配超过目标。
- 通过验证命令：`pnpm exec vitest run packages/runtime/__tests__/reactive-kernel.graph.spec.ts --project unit-jsdom`
- 模拟策略：使用真实 Graph，无 mock。

## 步骤

1. 添加代际复用、顺序、去重、释放和 debug 统计失败测试。
2. 记录任务 1 中 Graph 工作负载基线。
3. 将节点/边 ID 和 arena 改为紧凑表示，逐步更新图内算法。
4. 运行 Graph 与 kernel signal/effect 相邻测试。
5. 对比任务 1 报告，确认吞吐不降且分配/堆峰值下降。

## 验证

- 运行：`pnpm exec vitest run packages/runtime/__tests__/reactive-kernel.graph.spec.ts packages/runtime/__tests__/reactive-kernel.signal-effect-computed.spec.ts --project unit-jsdom`
- 运行：`node --expose-gc scripts/reactive-kernel-performance.mjs --scenario graph --compare scripts/reactive-kernel-performance-baseline.json`
- 预期：功能测试退出码 0；Graph 场景在预算内，节点/边释放回到基线。
- 所需证据：失败与通过测试、Graph debug 统计、基准前后中位数/堆增量、审查后的 Graph 差异。

## 完成

确认所有 Graph 句柄调用已迁移、无双表示适配、性能报告通过后删除任务文件。

