# 任务 11: 性能准入与全量回归

批次：【批次 11】 依赖批次 10

状态：未开始

目的：删除残留死代码，固化性能/内存/体积预算，并执行全仓回归证明无代理重构完成。

来源任务：无

预计会话范围：最终清理、预算定稿和跨包验证；不再引入新架构或兼容层。

## 文件

- 修改：`scripts/reactive-kernel-performance-baseline.json`
- 修改：`scripts/reactive-kernel-performance-budget.json`
- 修改：`scripts/runtime-size-baseline.json`
- 修改：`scripts/runtime-size-budget.json`
- 修改：`scripts/tree-shaking-baseline.json`
- 修改：`scripts/tree-shaking-budget.json`
- 修改：`scripts/check-compat-cleanup.js`
- 修改：`scripts/__tests__/reactive-kernel-performance.spec.ts`
- 修改：`scripts/__tests__/js-framework-performance.spec.ts`
- 参考：`packages/runtime/src/runtime-core/compiled.ts`
- 参考：`packages/runtime/src/runtime-core/reactive-kernel/`

## 上下文

- 预算必须基于同一机器多轮中位数更新，记录相对旧基线的真实改善，不放宽无关现有预算。
- 最终源码扫描只禁止响应式与 compiled props/store Proxy；计划范围外 Proxy 必须在清理检查中按精确文件白名单说明用途。

## 测试计划

- 行为：性能、内存、体积、tree-shaking、类型、Rust、unit 和 E2E 门禁全部通过，且无重复内核/响应式 Proxy 残留。
- 失败验证测试：先更新 cleanup 检查与预算测试，使残留文件、符号或旧预算触发失败。
- 失败验证命令：`pnpm run check:compat-cleanup && node scripts/reactive-kernel-performance.mjs --compare scripts/reactive-kernel-performance-baseline.json --budget scripts/reactive-kernel-performance-budget.json`
- 预期失败原因：旧模块/导出或尚未定稿的预算仍存在。
- 通过验证命令：依次运行下方全部验证命令。
- 模拟策略：全部使用真实构建、运行时、浏览器测试和本地基准，无 mock。

## 步骤

1. 强化 cleanup 检查，禁止废弃导出、第二套订阅图和响应式 Proxy 文件。
2. 删除未被任何入口引用的 facade、helper、测试夹具和注释。
3. 多轮运行新旧可比基准，更新基线与不回退预算。
4. 更新 runtime size/tree-shaking 基线，审查 compiler entry 模块组成。
5. 运行 TypeScript、Rust、unit、Text、E2E、性能和 release 检查。

## 验证

- 运行：`pnpm run check && pnpm run check:compat-cleanup && pnpm run build-dts`
- 运行：`cargo test --manifest-path packages/swc-plugin-rue/Cargo.toml`
- 运行：`pnpm run test-unit && pnpm run test:text-unit && pnpm run test-e2e`
- 运行：`node --expose-gc scripts/reactive-kernel-performance.mjs --compare scripts/reactive-kernel-performance-baseline.json --budget scripts/reactive-kernel-performance-budget.json --output temp/performance/reactive-final.json`
- 运行：`pnpm run benchmark:js-framework:check && pnpm run size-runtime && pnpm run size:tree-shaking:check`
- 预期：所有命令退出码 0；路径/数组/Graph 核心指标优于旧基线且均在预算内；无资源增长和禁止模块。
- 所需证据：完整命令矩阵、测试数量、性能前后表、堆峰值、bundle 各项尺寸、残留 Proxy 白名单、最终差异审查。

## 完成

只有全部门禁有本次运行证据、旧实现已删除且性能改善量化后才能删除任务文件。
