# 任务 3: 实现无分配路径节点

批次：【批次 3】 依赖批次 2

状态：未开始

目的：用路径 Trie/游标替代数组复制、字符串规范化和祖先链不可变克隆，为 Proxy-free 编译访问提供底座。

来源任务：无

预计会话范围：仅重构 Signal 路径模型及直接测试；暂时保留现有 Proxy 调用方以便下一批切换。

## 文件

- 修改：`packages/runtime/src/runtime-core/reactive-kernel/signal.ts`
- 修改：`packages/runtime/src/runtime-core/reactive-kernel/index.ts`
- 修改：`packages/runtime/src/runtime-core/js-reactive/types.ts`
- 修改：`packages/runtime/__tests__/reactive-kernel.reactive-proxy.spec.ts`
- 新建：`packages/runtime/__tests__/reactive-kernel.path-trie.spec.ts`

## 上下文

- 删除 `signalPathKey()` 的字符串键和热路径 `normalizeSignalPath().map()`；路径节点按 `PropertyKey` 建 Trie，静态路径可预解析为稳定 token。
- 新增 `mutatePath(path, mutator)`；`setPath/updatePath/mutatePath` 原地修改并触发精确节点、必要祖先和已登记 descendant watcher。

## 测试计划

- 行为：深路径读写不分配临时路径数组、不发生字符串键碰撞，并只唤醒相关订阅者。
- 失败验证测试：新增“特殊字符串/Symbol 路径不碰撞”“万级数组单项原地更新”“静态 token 重用”“路径节点释放”。
- 失败验证命令：`pnpm exec vitest run packages/runtime/__tests__/reactive-kernel.path-trie.spec.ts --project unit-jsdom`
- 预期失败原因：当前实现使用数组与 `map/join` 字符串键，并复制整个祖先链。
- 通过验证命令：`pnpm exec vitest run packages/runtime/__tests__/reactive-kernel.path-trie.spec.ts packages/runtime/__tests__/reactive-kernel.reactive-proxy.spec.ts --project unit-jsdom`
- 模拟策略：真实 Signal、Graph 和 Effect，无 mock。

## 步骤

1. 写入碰撞、精确触发、原地写入、token 复用和释放失败测试。
2. 引入路径节点/token 类型以及一次性字符串路径解析边界。
3. 改写 get/peek/set/update/trigger/mutate 路径操作与 affected-node 搜索。
4. 删除不可变克隆和全局 Symbol 字符串 ID；更新手写 kernel 类型。
5. 运行功能和深路径/数组基准，确认热路径分配下降。

## 验证

- 运行：`pnpm exec vitest run packages/runtime/__tests__/reactive-kernel.path-trie.spec.ts packages/runtime/__tests__/reactive-kernel.reactive-proxy.spec.ts packages/runtime/__tests__/reactive-kernel.watch-resource-log.spec.ts --project unit-jsdom`
- 运行：`node --expose-gc scripts/reactive-kernel-performance.mjs --scenario path --compare scripts/reactive-kernel-performance-baseline.json`
- 预期：测试退出码 0；无路径碰撞；万级数组更新不复制整数组；路径场景通过预算。
- 所需证据：预期失败、通过数量、路径节点释放统计、基准时间与堆增量。

## 完成

仅当数组/字符串路径旧热路径已移除且所有路径 API 共用 Trie 时删除本任务文件。

