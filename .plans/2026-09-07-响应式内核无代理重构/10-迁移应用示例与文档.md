# 任务 10: 迁移应用示例与文档

批次：【批次 10】 依赖批次 9

状态：未开始

目的：把仓库内 reactive/readonly/toRef/toRefs/isProxy 示例和直接调用迁移到 Signal/编译状态模型，明确新框架语义。

来源任务：无

预计会话范围：仅修改 app、examples 和 docs 内容及其页面测试；在 Store/runtime 迁移完成后统一验证应用构建。

## 文件

- 修改：`app/pages/site/SiteHome.tsx`
- 修改：`app/pages/examples/home-demos/GlobalComponentRegistrationDemo.tsx`
- 修改：`app/pages/examples/home-demos/MapListRenderingDemo.tsx`
- 修改：`app/pages/examples/home-demos/ReactStyleConditionalDemo.tsx`
- 修改：`app/pages/examples/home-demos/ChildToParentNotifyDemo.tsx`
- 修改：`app/pages/examples/home-demos/BasicTodoListDemo.tsx`
- 修改：`app/pages/examples/home-demos/LocalTodoListDemo.tsx`
- 修改：`app/pages/examples/ToRef.tsx`
- 修改：`app/pages/examples/ToRefs.tsx`
- 修改：`app/pages/examples/IsRef.tsx`
- 修改：`app/pages/examples/IsProxy.tsx`
- 修改：`app/pages/examples/IsReadonly.tsx`
- 修改：`docs/guide/extras/reactivity-in-depth.md`
- 修改：`docs/guide/best-practices/performance.md`

## 上下文

- 删除 Vue Proxy 教学与 `isProxy` 页面入口；文档说明对象状态必须使用 Signal 显式路径 API或编译期 `useState`，未知逃逸不受支持。
- 不直接编辑生成文件 `docs/search-index.json`；由现有生成命令重建。

## 测试计划

- 行为：所有示例构建、交互与代码展示使用新 API，文档不再声称 deep Proxy reactive。
- 失败验证测试：更新现有页面相关测试/构建断言，使废弃导入导致预期失败。
- 失败验证命令：`pnpm run check && pnpm run app-build`
- 预期失败原因：页面仍导入已删除的 reactive Proxy API。
- 通过验证命令：`pnpm run docs-search-index && pnpm run check && pnpm run app-build`
- 模拟策略：真实 TypeScript、SWC 和 Vite 构建，无 mock。

## 步骤

1. 列出 app/examples/docs 中全部废弃 API 引用并保存清单。
2. 迁移模块级对象状态到 Signal 路径 API，组件本地状态优先用编译 `useState`。
3. 删除 Proxy 专属示例或改写为“无代理状态模型”页面。
4. 重写响应式原理和性能文档，明确支持与诊断边界。
5. 重建搜索索引并运行检查、应用构建和相关页面测试。

## 验证

- 运行：`rg -n '\b(reactive|shallowReactive|readonly|shallowReadonly|propsReactive|isProxy|toRaw|toRefs?)\b' app examples docs --glob '!docs/search-index.json'`
- 运行：`pnpm run docs-search-index && pnpm run check && pnpm run app-build`
- 预期：扫描只剩明确讲解已删除 API 的迁移说明；构建命令退出码 0。
- 所需证据：迁移前后引用清单、检查和构建退出码、生成索引状态、抽查交互页面结果。

## 完成

所有仓库示例和用户文档都以新模型为准且构建通过后删除任务文件。
