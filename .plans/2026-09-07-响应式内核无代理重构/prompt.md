# 响应式内核无代理重构任务提示词

按顺序复制以下提示到独立对话框执行。执行前先阅读 `plan.md` 和对应任务文件；`plan.md` 只是整体架构指南，不是进度来源，不要修改它的任何部分。

# 批次 1

## 任务 1: 建立响应式性能基线【批次 1】

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-09-07-响应式内核无代理重构/1-建立响应式性能基线.md`。

开始前先阅读 `.plans/2026-09-07-响应式内核无代理重构/plan.md` 和该任务文件，确认依赖已满足。按任务文件中的测试驱动开发/验证计划实施，尽可能只处理该任务范围，遇到阻塞可以扩大任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的原因无法运行验证，可以解除 `已阻塞`，修改状态为 `已完成，待回归`。
注意，`.plans/2026-09-07-响应式内核无代理重构/plan.md` 为只读，不要修改该文件的任何部分。
```

# 批次 2

## 任务 2: 压缩依赖图热路径【批次 2】依赖批次 1

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-09-07-响应式内核无代理重构/2-压缩依赖图热路径.md`。

开始前先阅读 `.plans/2026-09-07-响应式内核无代理重构/plan.md` 和该任务文件，确认依赖已满足。按任务文件中的测试驱动开发/验证计划实施，尽可能只处理该任务范围，遇到阻塞可以扩大任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的原因无法运行验证，可以解除 `已阻塞`，修改状态为 `已完成，待回归`。
注意，`.plans/2026-09-07-响应式内核无代理重构/plan.md` 为只读，不要修改该文件的任何部分。
```

# 批次 3

## 任务 3: 实现无分配路径节点【批次 3】依赖批次 2

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-09-07-响应式内核无代理重构/3-实现无分配路径节点.md`。

开始前先阅读 `.plans/2026-09-07-响应式内核无代理重构/plan.md` 和该任务文件，确认依赖已满足。按任务文件中的测试驱动开发/验证计划实施，尽可能只处理该任务范围，遇到阻塞可以扩大任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的原因无法运行验证，可以解除 `已阻塞`，修改状态为 `已完成，待回归`。
注意，`.plans/2026-09-07-响应式内核无代理重构/plan.md` 为只读，不要修改该文件的任何部分。
```

# 批次 4

## 任务 4: 统一编译与通用内核【批次 4】依赖批次 3

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-09-07-响应式内核无代理重构/4-统一编译与通用内核.md`。

开始前先阅读 `.plans/2026-09-07-响应式内核无代理重构/plan.md` 和该任务文件，确认依赖已满足。按任务文件中的测试驱动开发/验证计划实施，尽可能只处理该任务范围，遇到阻塞可以扩大任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的原因无法运行验证，可以解除 `已阻塞`，修改状态为 `已完成，待回归`。
注意，`.plans/2026-09-07-响应式内核无代理重构/plan.md` 为只读，不要修改该文件的任何部分。
```

# 批次 5

## 任务 5: 编译状态成员读取【批次 5】依赖批次 4

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-09-07-响应式内核无代理重构/5-编译状态成员读取.md`。

开始前先阅读 `.plans/2026-09-07-响应式内核无代理重构/plan.md` 和该任务文件，确认依赖已满足。按任务文件中的测试驱动开发/验证计划实施，尽可能只处理该任务范围，遇到阻塞可以扩大任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的原因无法运行验证，可以解除 `已阻塞`，修改状态为 `已完成，待回归`。
注意，`.plans/2026-09-07-响应式内核无代理重构/plan.md` 为只读，不要修改该文件的任何部分。
```

# 批次 6

## 任务 6: 编译状态写入与变异【批次 6】依赖批次 5

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-09-07-响应式内核无代理重构/6-编译状态写入与变异.md`。

开始前先阅读 `.plans/2026-09-07-响应式内核无代理重构/plan.md` 和该任务文件，确认依赖已满足。按任务文件中的测试驱动开发/验证计划实施，尽可能只处理该任务范围，遇到阻塞可以扩大任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的原因无法运行验证，可以解除 `已阻塞`，修改状态为 `已完成，待回归`。
注意，`.plans/2026-09-07-响应式内核无代理重构/plan.md` 为只读，不要修改该文件的任何部分。
```

# 批次 7

## 任务 7: 移除编译 Props 代理【批次 7】依赖批次 6

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-09-07-响应式内核无代理重构/7-移除编译Props代理.md`。

开始前先阅读 `.plans/2026-09-07-响应式内核无代理重构/plan.md` 和该任务文件，确认依赖已满足。按任务文件中的测试驱动开发/验证计划实施，尽可能只处理该任务范围，遇到阻塞可以扩大任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的原因无法运行验证，可以解除 `已阻塞`，修改状态为 `已完成，待回归`。
注意，`.plans/2026-09-07-响应式内核无代理重构/plan.md` 为只读，不要修改该文件的任何部分。
```

# 批次 8

## 任务 8: 删除响应式 Proxy 公共面【批次 8】依赖批次 7

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-09-07-响应式内核无代理重构/8-删除响应式Proxy公共面.md`。

开始前先阅读 `.plans/2026-09-07-响应式内核无代理重构/plan.md` 和该任务文件，确认依赖已满足。按任务文件中的测试驱动开发/验证计划实施，尽可能只处理该任务范围，遇到阻塞可以扩大任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的原因无法运行验证，可以解除 `已阻塞`，修改状态为 `已完成，待回归`。
注意，`.plans/2026-09-07-响应式内核无代理重构/plan.md` 为只读，不要修改该文件的任何部分。
```

# 批次 9

## 任务 9: 迁移 Store 与运行时消费者【批次 9】依赖批次 8

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-09-07-响应式内核无代理重构/9-迁移Store与运行时消费者.md`。

开始前先阅读 `.plans/2026-09-07-响应式内核无代理重构/plan.md` 和该任务文件，确认依赖已满足。按任务文件中的测试驱动开发/验证计划实施，尽可能只处理该任务范围，遇到阻塞可以扩大任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的原因无法运行验证，可以解除 `已阻塞`，修改状态为 `已完成，待回归`。
注意，`.plans/2026-09-07-响应式内核无代理重构/plan.md` 为只读，不要修改该文件的任何部分。
```

# 批次 10

## 任务 10: 迁移应用示例与文档【批次 10】依赖批次 9

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-09-07-响应式内核无代理重构/10-迁移应用示例与文档.md`。

开始前先阅读 `.plans/2026-09-07-响应式内核无代理重构/plan.md` 和该任务文件，确认依赖已满足。按任务文件中的测试驱动开发/验证计划实施，尽可能只处理该任务范围，遇到阻塞可以扩大任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的原因无法运行验证，可以解除 `已阻塞`，修改状态为 `已完成，待回归`。
注意，`.plans/2026-09-07-响应式内核无代理重构/plan.md` 为只读，不要修改该文件的任何部分。
```

# 批次 11

## 任务 11: 性能准入与全量回归【批次 11】依赖批次 10

```text
请使用技能 ./skills/do-task-plans/SKILL.md 执行 `.plans/2026-09-07-响应式内核无代理重构/11-性能准入与全量回归.md`。

开始前先阅读 `.plans/2026-09-07-响应式内核无代理重构/plan.md` 和该任务文件，确认依赖已满足。按任务文件中的测试驱动开发/验证计划实施，尽可能只处理该任务范围，遇到阻塞可以扩大任务范围。
任务获得所需验证证据后，在最终回复中报告完成证据并删除该任务文件；如果被阻塞，只在任务文件中更新为 `已阻塞` 并记录具体原因和已运行的检查；如果因为无关本次修改的原因无法运行验证，可以解除 `已阻塞`，修改状态为 `已完成，待回归`。
注意，`.plans/2026-09-07-响应式内核无代理重构/plan.md` 为只读，不要修改该文件的任何部分。
```
