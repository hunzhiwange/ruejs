# 任务 3: 体积审计与全量回归

批次：【批次 3】 依赖批次 1,2

状态：未开始

目的：量化 helper 删除后的产物影响，更新必要基线，并证明完整仓库没有行为回退。

来源任务：无

预计会话范围：仅做构建、体积/tree-shaking 审计、全量回归和由实际产物变化直接要求的基线更新，不再扩大实现范围。

## 文件

- 修改：`scripts/runtime-size-baseline.json`（仅当审计确认稳定改善且脚本要求更新）
- 修改：`scripts/tree-shaking-baseline.json`（仅当生成入口变化导致审计要求更新）
- 测试：`scripts/__tests__/runtime-size-audit.spec.ts`
- 测试：`scripts/__tests__/tree-shaking-audit.spec.ts`

## 上下文

- 预期收益来自纯编译 JSX 不再因 `vapor` 被提升到 `internal/component`，不是移除 `_$compiledRoot` 的生命周期成本。基线只能根据当前脚本生成的真实证据更新。

## 验证计划

- 行为：构建产物不再含编译期 `vapor` ABI；纯 compiler 场景体积不回退，完整测试保持通过。
- 基线来源：使用仓库已提交的 `scripts/runtime-size-baseline.json` 与 `scripts/tree-shaking-baseline.json`；实施后运行对应审计并比较。
- 通过验证命令：`pnpm run build && pnpm run check && pnpm run test-unit && pnpm run size-runtime && pnpm run size:tree-shaking:check`
- 预期：所有命令退出码 0；compiler-only 场景不增大；若存在改善，报告 raw/gzip/brotli 的前后差值。

## 步骤

1. 构建所有相关包，确保发布入口解析到新的 helper 契约。
2. 执行 runtime size 与 tree-shaking 审计，对比实施前结果并定位差异到具体入口。
3. 仅在审计工具要求且变化由本任务直接造成时更新基线，再次运行 check 模式。
4. 运行 TypeScript、单元测试和兼容清理检查。
5. 审查最终差异，确认没有删除或修改 runtime-core portable Vapor 路径。

## 验证

- 运行：`pnpm run build && pnpm run check && pnpm run test-unit && pnpm run size-runtime && pnpm run size:tree-shaking:check`
- 预期：全部退出码 0，无新增失败或预算回退。
- 所需证据：各命令退出码、测试通过/失败数量、体积审计前后 raw/gzip/brotli 数值、最终 `git diff --check` 结果，以及剩余 `vapor` 命中的分类审查清单。

## 完成

只有当完整验证通过、体积无回退且性能表述与实际审计数据一致时，任务才可标记为已完成；没有可测改善时明确报告“接口精简，无可测性能提升”。
