{
  "name": "@rue-js/app-rust",
  "version": "0.0.1",
  "private": true,
  "description": "Rust business logic for Rue app (WASM)",
  "files": [
    "pkg"
  ],
  "types": "./pkg/app_rust.d.ts",
  "exports": {
    ".": {
      "development": "./pkg/app_rust.js",
      "import": "./pkg/app_rust.js",
      "default": "./pkg/app_rust.js"
    }
  },
  "scripts": {
    "build": "wasm-pack build --release --target bundler --out-dir pkg --features dev --features wgpu_rs",
    "build-dev": "wasm-pack build --release --target bundler --out-dir pkg --features dev --features wgpu_rs",
    "dev": "wasm-pack watch --features dev --features wgpu_rs",
    "docs": "cargo doc --no-deps",
    "check": "cargo check",
    "fmt": "cargo fmt"
  }
}
